# Agent Handoffs

Create one short Markdown file per meaningful handoff, for example:

`YYYY-MM-DD-implementation-to-benchmark.md`

Each handoff records the agent role, input and output artifacts, files changed, verification command, open risks, and next responsible role.

Handoffs are evidence of coordination, not a substitute for tests or review.
